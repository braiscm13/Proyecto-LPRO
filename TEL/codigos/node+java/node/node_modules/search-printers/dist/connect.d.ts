/// <reference types="node" />
interface Options {
    host: string;
    port: number;
    timeout: number;
    buffer: Buffer;
}
export interface Printer {
    host: string;
    port: number;
    name: string;
}
declare function tryConnect({ host, port, timeout, buffer }: Options): Promise<Printer>;
export { tryConnect };
