/// <reference types="node" />
import { Printer } from "./connect";
declare function getPrinters(options: {
    timeout?: number;
    port?: number;
    buffer?: Buffer;
}): Promise<Printer[]>;
export default getPrinters;
